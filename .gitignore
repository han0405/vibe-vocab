# Local activation flags (per-project / per-user state, never shared)
.vibe-vocab-on
.vibe-vocab-always
.vibe-vocab-rate
.vibe-vocab-known
.vibe-vocab-level

# Runtime output — the user's own word log / focus pack / export, written by the hooks
vocab-log.md
vocab-focus.md
vocab-anki.csv

# Codex adapter writes a managed block into the project's AGENTS.md; ignore it
# in this repo so a local `npm run codex:install` dogfood run isn't committed.
AGENTS.md

# Scratch fixtures created by the prompt-evaluation pass
scripts/fixtures/eval-*.txt

# Node
node_modules/
npm-debug.log*

# Scratch: repomix bundle output
repomix-output.xml
